# React-Native-Gym-Macro-Tracker
A Macro Tracker without all the noise of regular gym apps. Calculate. Track. Analyze

Features:
-Formik for forms and input validation 
-Hooks used for state management so that data can be passed and changed throughout the code
